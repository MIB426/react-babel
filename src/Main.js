import React, { Component } from 'react'
import config from './config.json';

class Main extends Component {
    render() {
        return (
            <div>
                {config.MeeageReact}
            </div>
        );
    }
}

export default Main